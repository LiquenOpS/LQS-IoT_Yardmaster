# Copy the whole config.example folder to config/ and edit.

# IOTA — host/port where Pylon IoT Agent North runs (Yardmaster provisions here)
IOTA_HOST="localhost"
IOTA_NORTH_PORT="4041"
IOTA_SOUTH_PORT="7896"

# Yardmaster (this process) — address reachable by Pylon for command callbacks
YARDMASTER_HOST="host.docker.internal"
YARDMASTER_PORT="8080"

# FIWARE — apikey must match Pylon service group (e.g. YardmasterKey)
# FIWARE_SERVICE: Orion allows alphanumeric + underscore only, no hyphen
API_KEY="YardmasterKey"
FIWARE_SERVICE="lqs_iot"
FIWARE_SERVICEPATH="/"
HEADER_CONTENT_TYPE="Content-Type: application/json"
HEADER_FIWARE_SERVICE="fiware-service: ${FIWARE_SERVICE}"
HEADER_FIWARE_SERVICEPATH="fiware-servicepath: ${FIWARE_SERVICEPATH}"

# Capabilities (true/false)
ENABLE_SIGNAGE=true
ENABLE_LED_STRIP=true

# Signage -> anthias
ANTHIAS_BASE_URL="http://localhost:8000/api/v2/assets"

# LEDStrip -> Glimmer (v2026-02-05, default api_port 1129)
GLIMMER_BASE_URL="http://localhost:1129"

# Orion (for debug scripts)
ORION_HOST="${ORION_HOST:-localhost}"
ORION_PORT="${ORION_PORT:-1026}"
